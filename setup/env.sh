# environment vars

# location of the WAVED/WAVEMQ/WV binaries
export BIN_LOCATION=/usr/local/bin
# WAVED configuration folder
export WAVED_CFG_LOCATION=/etc/xbos/waved
# where to store the wave graph
export WAVED_STORAGE_LOCATION=/var/lib/xbos/wave
export WAVEMQ_CFG_LOCATION=/etc/xbos/wavemq
export WAVEMQ_STORAGE_LOCATION=/var/lib/xbos/wavemq

# if true, replace existing wavemq/waved/wv binaries
export UPGRADE=false
# if true, replace existing configuration
export OVERRIDE_CONFIG=false
